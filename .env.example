# Variables de Entorno - Ejemplo
# Copia este archivo a .env para desarrollo local

# Puerto del servidor
PORT=3000

# URL de conexión a PostgreSQL
# Formato: postgresql://usuario:contraseña@host:puerto/nombre_base_datos
# LOCAL: postgresql://usuario:password@localhost:5432/rrhh_db
# RENDER: Usa la "Internal Database URL" que te proporciona Render automáticamente
DATABASE_URL=postgresql://usuario:password@localhost:5432/rrhh_db

# Entorno de ejecución (development o production)
NODE_ENV=development

# === INSTRUCCIONES PARA RENDER ===
# 1. En Render Dashboard, ve a tu Web Service
# 2. Click en "Environment" en el menú lateral
# 3. Agrega estas variables:
#    - DATABASE_URL: Copia la "Internal Database URL" de tu PostgreSQL en Render
#    - NODE_ENV: production
# 4. NO agregues PORT (Render lo asigna automáticamente)
# 5. Guarda los cambios y redeploy automáticamente
